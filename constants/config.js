// Constantes de configuración sensibles
// Este archivo contiene URLs y configuraciones que no deben estar hardcodeadas en el código fuente

export const CLOUDINARY_CONFIG = {
  // URL del placeholder por defecto para fotos de perfil
  PLACEHOLDER_URL:
    "https://res.cloudinary.com/dtg2mkilx/image/upload/placeholder_jrnkvd.png",
};

// Aquí se pueden agregar más constantes de configuración en el futuro
export const API_CONFIG = {
  // Ejemplo para futuras configuraciones de API
  // BASE_URL: process.env.API_BASE_URL || "http://localhost:8080",
};

export const STORAGE_KEYS = {
  // Claves para el almacenamiento local
  // USER_TOKEN: "user_token",
  // USER_PREFERENCES: "user_preferences",
};
